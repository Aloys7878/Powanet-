POWANET v4.0 Full Build (Demo-ready)

How to use:
1. unzip, run `npm install` then `npm run dev` for local testing.
2. Upload the ZIP to Pi App Studio using "Upload code" (App Studio supports code upload).
3. In Pi App Studio edit app: replace app with uploaded code, then publish to Testnet.

This bundle contains:
- pages/ (index, api stubs)
- components/ (EgoChat, NetworkGrid, Wallet)
- public/assets (SVG placeholders)
- styles/globals.css
POWANET v4 — Quantum Global. Build a mobile-first app that is secure, modular, and Web3-ready. Core features: 25 global networks (Health, Finance, Energy, Agri, Transport, Education, AIHub, CryptoNet, etc.), dual-mode Testnet/Mainnet, Pi wallet integration, POA token economy, staking & rewards, multilingual EGO Assistant (voice + text), Global Conversion Value (GCV: 1 π = 314159 USD), responsive dashboard, real original images for networks, and anti-black-screen protections. Provide safe defaults, demo data for Testnet, and API stubs for production endpoints. Generate UI components, accessible layout, and a minimal integration guide for replacing demo endpoints with real APIs (OpenAI, Mapbox, Pi SDK).
