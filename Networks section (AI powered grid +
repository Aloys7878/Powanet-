import React from "react";
import { motion } from "framer-motion";

const networks = [
  { name: "HealthNet", icon: "❤️", status: "online" },
  { name: "EcoNet", icon: "🌱", status: "online" },
  { name: "EduNet", icon: "🎓", status: "online" },
  { name: "AgriNet", icon: "🌾", status: "active" },
  { name: "FinNet", icon: "💰", status: "upgrading" },
  { name: "AIHub", icon: "🤖", status: "online" },
  { name: "TradeNet", icon: "🛒", status: "online" },
  { name: "TechNet", icon: "💻", status: "active" },
  { name: "PeaceNet", icon: "🌍", status: "stable" },
  { name: "CryptoNet", icon: "🪙", status: "updating" },
];

export default function NetworkGrid() {
  return (
    <div className="grid grid-cols-2 md:grid-cols-3 gap-4 p-4">
      {networks.map((net, i) => (
        <motion.div
          key={i}
          whileHover={{ scale: 1.05 }}
          className="p-4 rounded-2xl shadow-xl text-center bg-gradient-to-br from-blue-800 to-gray-900 text-white"
        >
          <div className="text-4xl mb-2">{net.icon}</div>
          <h3 className="font-bold">{net.name}</h3>
          <p className="text-sm opacity-75">Status: {net.status}</p>
        </motion.div>
      ))}
    </div>
  );
}
